# Example env file (do not commit your real `.env`)

# Google Maps Platform API key (Photorealistic 3D Tiles / Map Tiles API)
GMP_API_KEY="replace_me"

# Optional: Path to Chrome/Chromium (used by `pnpm data:iso:gmp:preview`)
# CHROME_EXECUTABLE_PATH="/Applications/Google Chrome.app/Contents/MacOS/Google Chrome"

# Optional: HuggingFace token (only needed for gated/private models)
# HUGGINGFACE_HUB_TOKEN="hf_replace_me"

