{
  "name": "@tashkent-voxel-vision/data",
  "private": true,
  "version": "0.0.0",
  "dependencies": {
    "ajv": "^8.17.1",
    "ajv-formats": "^3.0.1"
  },
  "scripts": {
    "lint": "eslint -c ../../.eslintrc.cjs .",
    "test": "node --test test/data-release-manifest.test.mjs",
    "manifest:validate": "node scripts/manifest-validate.mjs",
    "release:init": "node scripts/release-init.mjs",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  }
}
